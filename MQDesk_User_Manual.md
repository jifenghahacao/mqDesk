# MQDesk 零基础用户操作手册

> **版本**：v0.1.0（MVP）  
> **适用对象**：完全不懂 RabbitMQ、不懂命令行、第一次使用 MQDesk 的用户  
> **阅读建议**：按顺序阅读，遇到看不懂的术语可先翻到文末「附录 → 术语解释」。

---

## 1. 产品概述

### 1.1 MQDesk 是什么？

MQDesk 是一款**桌面版 RabbitMQ 可视化管控工具**。

你可以把它理解成一个"**消息队列的图形化管理小助手**"。它能帮你：

- 不用记命令，就能连接 RabbitMQ 服务；
- 一眼看出队列是否堆积、有没有人消费；
- 像发微信一样发送测试消息；
- 查看消息发送记录，追踪消息去向。

### 1.2 它能帮你解决什么问题？

| 场景 | 没有 MQDesk 时 | 有了 MQDesk 后 |
|------|----------------|----------------|
| 想查看队列有多少消息 | 要打开英文管理后台，找半天 | 打开软件总览页，数字直接显示 |
| 怀疑消息没发出去 | 要写代码测试或查日志 | 点几下就能发一条测试消息 |
| 队列消息堆积了 | 看不到，等问题爆发 | 健康状态变红/黄，提前预警 |
| 忘了连接密码 | 明文写在配置文件里 | 本地加密保存，更安全 |

### 1.3 适合谁用？

- 刚接触 RabbitMQ 的新手开发者；
- 需要临时查看/测试消息队列的测试人员；
- 不想折腾英文管理后台的运维人员；
- 学习消息队列课程的学生。

---

## 2. 安装与初始化

### 2.1 环境准备

安装 MQDesk 前，请确认你的电脑满足以下条件：

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11（64 位） |
| 内存 | 至少 4GB |
| 硬盘空间 | 至少 200MB 可用空间 |
| 网络 | 能访问你要连接的 RabbitMQ 服务器 |
| 依赖组件 | 已内置，无需单独安装 |

> **什么是 RabbitMQ？** 它是一种"消息中间件"，负责在不同程序之间传递消息。如果你还没安装 RabbitMQ，可以先让同事或运维给你一个连接地址，再使用 MQDesk。

### 2.2 安装步骤

#### 第一步：获取安装包

从发布页面下载安装文件：

```
MQDesk_0.1.0_x64-setup.exe
```

> 📸 **截图位置**：下载页面或安装包文件图标  
> **标注**：用红色方框标出安装包文件名。

#### 第二步：运行安装程序

1. 双击 `MQDesk_0.1.0_x64-setup.exe`；
2. 如果 Windows 弹出"是否允许此应用对你的设备进行更改"，点击「是」；
3. 选择安装路径（建议保持默认），点击「下一步」；
4. 等待安装完成，点击「完成」。

> 📸 **截图位置**：安装向导界面  
> **标注**：① 安装路径输入框 ②「下一步」按钮 ③ 进度条。

#### 第三步：首次启动

1. 安装完成后，桌面会出现 MQDesk 图标；
2. 双击图标启动软件；
3. 首次启动会弹出主界面。

> 📸 **截图位置**：MQDesk 首次启动后的主界面  
> **标注**：用红色方框标出窗口整体区域。

### 2.3 首次启动配置

第一次打开 MQDesk，你会看到「连接管理」页面。此时还没有保存任何连接。

> 📸 **截图位置**：连接管理页面（未保存任何连接时）  
> **标注**：①「新建连接」按钮 ② 页面标题「连接管理」。

---

## 3. 界面导览

### 3.1 主界面整体布局

MQDesk 主界面分为四个主要区域：

```
┌─────────────────────────────────────────────┐
│  ① 标题栏（Logo + 窗口控制按钮）               │
├──────────┬──────────────────────────────────┤
│          │                                  │
│  ② 侧边栏 │           ③ 内容区              │
│ （导航）  │         （功能页面）             │
│          │                                  │
└──────────┴──────────────────────────────────┘
```

> 📸 **截图位置**：MQDesk 主界面  
> **标注**：用红色方框分别标出①标题栏、②侧边栏、③内容区。

### 3.2 ① 标题栏

标题栏位于窗口最顶部，包含：

| 元素 | 说明 |
|------|------|
| MQDesk Logo | 点击可快速回到连接页 |
| 窗口标题 | 显示「MQDesk — RabbitMQ 可视化管控台」 |
| 最小化按钮 | 点击后窗口缩小到任务栏 |
| 最大化按钮 | 点击后窗口放大到全屏 |
| 关闭按钮 | 点击后软件最小化到系统托盘，不会真正退出 |

> 📸 **截图位置**：标题栏右侧三个按钮  
> **标注**：① 最小化 ② 最大化 ③ 关闭（可用箭头指示）。

### 3.3 ② 侧边栏

侧边栏位于窗口左侧，是功能导航入口：

| 按钮 | 名称 | 作用 |
|------|------|------|
| 🔌 连接 | 连接管理 | 新建、编辑、删除 RabbitMQ 连接 |
| 📊 总览 | 总览仪表盘 | 查看整体健康状态和关键数字 |
| 📋 队列 | 队列列表 | 查看所有队列及其健康状态 |
| ✉️ 消息 | 消息发送 | 发送测试消息并查看记录 |
| ⚙️ 设置 | 设置 | 切换主题、查看术语表 |

> 📸 **截图位置**：侧边栏  
> **标注**：用红色方框标出每个导航按钮，并标注序号①②③④⑤。

### 3.4 ③ 内容区

内容区会随侧边栏切换显示不同页面。当前选中的导航按钮会高亮显示。

> 📸 **截图位置**：点击「总览」后的内容区  
> **标注**：① 页面标题 ② 统计卡片 ③ 健康状态横幅。

### 3.5 系统托盘图标

关闭窗口后，MQDesk 会继续运行在右下角系统托盘中：

- **左键单击托盘图标**：显示主窗口；
- **右键单击托盘图标**：显示「显示窗口 / 退出」菜单。

> 📸 **截图位置**：Windows 右下角系统托盘区域  
> **标注**：① MQDesk 托盘图标 ② 右键菜单。

---

## 4. 功能详解

### 4.1 连接管理

#### 是什么？

「连接管理」用于保存你要管理的 RabbitMQ 服务地址、用户名、密码等信息。

#### 为什么需要它？

就像微信需要保存账号密码一样，MQDesk 也需要先保存 RabbitMQ 的连接信息，才能帮你查看队列、发送消息。

#### 怎么用？

##### 新建连接

1. 点击侧边栏「连接」；
2. 点击右上角「+ 新建连接」按钮；
3. 在弹出的表单中填写连接信息：

| 字段 | 填写说明 | 示例 |
|------|----------|------|
| 连接名称 | 给这个连接起个名字，方便识别 | 开发环境 |
| 主机地址 | RabbitMQ 服务器地址 | 127.0.0.1 或 mq.example.com |
| AMQP 端口 | 消息传输端口，默认 5672 | 5672 |
| 管理端口 | 网页管理接口端口，默认 15672 | 15672 |
| 管理协议 | 一般选 http | http |
| 虚拟空间 | 默认填写 / | / |
| 用户名 | RabbitMQ 登录用户名 | guest |
| 密码 | RabbitMQ 登录密码 | guest |

> 📸 **截图位置**：新建连接表单  
> **标注**：用红色方框标出每个输入框，并用箭头指向示例填写位置。

4. 填写完成后，点击「测试连接」；
5. 如果提示「已连接：...」，说明信息填对了；
6. 点击「保存并连接」，软件会自动连接并跳转到总览页。

> 📸 **截图位置**：测试连接成功提示  
> **标注**：①「测试连接」按钮 ② 成功提示文字。

##### 编辑 / 删除连接

1. 在连接列表中，点击某个连接卡片；
2. 点击「编辑」可修改连接信息；
3. 点击「删除」可删除该连接（会二次确认）。

> 📸 **截图位置**：连接卡片上的编辑/删除按钮  
> **标注**：①「编辑」按钮 ②「删除」按钮。

##### 切换连接

1. 点击侧边栏「连接」；
2. 点击想要使用的连接卡片；
3. 软件会自动连接该 RabbitMQ 服务。

---

### 4.2 总览仪表盘

#### 是什么？

「总览」是 MQDesk 的"首页"，用一句话和几个数字卡片告诉你 RabbitMQ 当前整体状态。

#### 为什么需要它？

就像手机健康 App 每天告诉你"步数、睡眠、心率"一样，总览让你不用逐个队列检查，就能快速判断系统是否健康。

#### 怎么用？

连接成功后，软件会自动跳转到总览页，显示以下内容：

| 区域 | 说明 |
|------|------|
| 健康状态横幅 | 一句话总结当前状态，例如"运行良好"或"存在堆积预警" |
| 队列总数 | 当前有多少个队列 |
| 交换机总数 | 当前有多少个交换机 |
| 待消费消息 | 所有队列中等待被消费的消息总数 |
| 在线消费者 | 当前有多少个消费者程序在线 |

> 📸 **截图位置**：总览仪表盘页面  
> **标注**：① 健康状态横幅 ② 四个统计卡片。

##### 健康状态说明

| 颜色 | 状态 | 含义 |
|------|------|------|
| 🟢 绿色 | 正常 | 队列有消费者，消息没有堆积 |
| 🟡 黄色 | 堆积预警 | 某个队列消息数超过 1000，需要关注 |
| 🔴 红色 | 无人消费 | 队列有消息，但没有消费者处理 |
| ⚪ 灰色 | 空闲 | 队列里没有消息，也没有消费者 |

---

### 4.3 队列列表

#### 是什么？

「队列」页面列出当前 RabbitMQ 中的所有队列，并显示每个队列的健康状态。

#### 为什么需要它？

当你怀疑某个业务出问题（例如订单没处理、短信没发送），可以先看队列列表，快速定位是不是消息卡住了。

#### 怎么用？

1. 点击侧边栏「队列」；
2. 页面会显示所有队列；
3. 队列按健康状态排序，红色/黄色会排在前面；
4. 在顶部搜索框输入队列名，可以过滤列表。

> 📸 **截图位置**：队列列表页面  
> **标注**：① 搜索框 ② 队列名称 ③ 健康状态颜色点。

##### 查看队列详情

1. 在队列列表中找到目标队列；
2. 点击该行；
3. 进入队列详情页。

> 📸 **截图位置**：队列列表中点击某一行  
> **标注**：用红色方框标出可点击区域，并用箭头指向详情页。

---

### 4.4 队列详情

#### 是什么？

「队列详情」展示某个具体队列的实时数据，包括消息数量、消费者数量、消息速率等。

#### 为什么需要它？

当你发现某个队列健康状态异常（红色/黄色），需要进入详情页看具体数字，判断问题有多严重。

#### 怎么用？

队列详情页包含：

| 区域 | 说明 |
|------|------|
| 健康状态卡片 | 显示当前队列健康状态 |
| 核心指标 | 待消费、处理中、消费者数、消息速率 |
| 速率折线图 | 近一段时间的消息流入/流出趋势 |
| 抓取预览 | 查看队列前几条消息内容，不会真正消费 |

> 📸 **截图位置**：队列详情页面  
> **标注**：① 健康状态 ② 核心指标 ③ 折线图 ④ 抓取预览按钮。

##### 抓取预览消息

1. 在队列详情页找到「抓取预览」区域；
2. 输入想看的消息数量（默认 10 条）；
3. 点击「抓取预览」按钮；
4. 消息内容会显示在下方，原来的消息仍保留在队列中。

> 📸 **截图位置**：抓取预览区域  
> **标注**：① 数量输入框 ②「抓取预览」按钮 ③ 消息内容列表。

---

### 4.5 消息发送

#### 是什么？

「消息」页面用于向 RabbitMQ 发送测试消息，并查看发送历史。

#### 为什么需要它？

当你开发了新功能，想验证"程序能不能收到消息"，或者想模拟某个业务事件时，可以在这里手动发一条消息。

#### 怎么用？

##### 发送一条消息

1. 点击侧边栏「消息」；
2. 选择发送模式：
   - **直发模式**：直接发送到指定队列；
   - **交换机模式**：发送到交换机，由路由键决定去向。
3. 填写目标队列名或路由键；
4. 选择消息内容类型（通常选 JSON）；
5. 填写消息内容；
6. 点击「发送」。

> 📸 **截图位置**：消息发送页面  
> **标注**：① 模式切换 ② 队列名/路由键输入框 ③ 内容类型 ④ 消息内容框 ⑤「发送」按钮。

##### 查看发送记录

发送成功后，页面下方的消息流会新增一条记录：

| 字段 | 说明 |
|------|------|
| 时间 | 发送时间 |
| 队列 | 消息目标队列 |
| 状态 | 已发送 / 已消费 / 堆积中 / 发送失败 |
| 摘要 | 消息内容前几个字 |

> 📸 **截图位置**：消息流列表  
> **标注**：① 时间 ② 队列名 ③ 状态标签 ④ 摘要。

---

### 4.6 设置

#### 是什么？

「设置」页面用于调整应用偏好和查看帮助信息。

#### 为什么需要它？

你可以在这里切换界面主题、学习 RabbitMQ 术语。

#### 怎么用？

##### 切换主题

1. 点击侧边栏「设置」；
2. 找到「外观」区域；
3. 点击「浅色」「暗黑」或「跟随系统」；
4. 界面主题会立即切换。

> 📸 **截图位置**：设置页外观区域  
> **标注**：①「浅色」按钮 ②「暗黑」按钮 ③「跟随系统」按钮。

##### 查看术语表

设置页下方有术语表，遇到不懂的词可以在这里查看大白话解释。

> 📸 **截图位置**：设置页术语表  
> **标注**：① 术语表表格。

---

## 5. 常见问题解答

### Q1：打开软件后提示"未连接到 RabbitMQ"怎么办？

**原因**：还没有选择或保存连接。  
**解决**：点击侧边栏「连接」→「新建连接」→ 填写信息 →「保存并连接」。

### Q2：测试连接提示"认证失败"怎么办？

**原因**：用户名或密码填错了。  
**解决**：检查用户名、密码是否正确，注意大小写。如果不确定，可以询问提供 RabbitMQ 地址的同事或运维。

### Q3：队列健康状态是红色，是什么意思？

**含义**：这个队列里有消息，但没有消费者在处理。  
**解决**：检查对应的消费者程序是否启动，或者是否配置错误。

### Q4：队列健康状态是黄色，是什么意思？

**含义**：这个队列的消息数超过了 1000 条，出现堆积。  
**解决**：加快消费速度，或增加消费者实例。

### Q5：发送消息后，状态一直显示"已发送"，不变成"已消费"？

**原因**：没有消费者程序处理这条消息。  
**解决**：启动对应的消费者程序，或检查路由键是否正确。

### Q6：关闭窗口后，MQDesk 还在运行吗？

**答案**：是的。关闭窗口会最小化到系统托盘，右键托盘图标选择「退出」才能真正关闭。

### Q7：软件右下角没有托盘图标？

**原因**：Windows 可能把图标折叠到「显示隐藏的图标」区域。  
**解决**：点击任务栏右下角的小箭头 ^，找到 MQDesk 图标，拖到常驻区域。

### Q8：界面出现「跳到主内容」字样？

**答案**：这是给键盘用户使用的无障碍快捷链接，按 Tab 键聚焦时会出现，平时不会显示。如果你一直看到它，请升级到最新版本。

### Q9：暗黑模式下某些区域还是白色的？

**原因**：早期版本可能存在主题适配不完整。  
**解决**：切换到浅色模式使用，或等待后续更新。

---

## 6. 附录

### 6.1 快捷键列表

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 在按钮/输入框之间切换焦点 |
| `Shift + Tab` | 反向切换焦点 |
| `Enter` / `Space` | 激活当前聚焦的按钮 |
| `Esc` | 关闭弹窗（如果有） |

### 6.2 术语解释

| 术语 | 大白话解释 |
|------|------------|
| RabbitMQ | 一个负责在不同程序之间传递消息的软件 |
| 队列（Queue） | 存放消息的"桶"，消费者从这里取消息 |
| 交换机（Exchange） | 消息的"分拣中心"，决定消息该进哪个队列 |
| 路由键（Routing Key） | 贴在消息上的"地址标签" |
| 消费者（Consumer） | 从队列取消息并处理的程序 |
| 待消费（Ready） | 已经躺在队列里、等着被取的消息 |
| 处理中（Unacked） | 已被取走但还没确认处理完的消息 |
| 虚拟空间（vhost） | 一块隔离的"房间"，不同项目互不干扰 |
| AMQP | RabbitMQ 用来传输消息的协议 |
| Publisher Confirm | 发送消息后，RabbitMQ 给的"收到回执" |

### 6.3 技术支持

如在使用过程中遇到问题，可通过以下方式获取帮助：

- **项目仓库**：`d:\project\RabbitConsumerHub-main`
- **项目文档**：
  - 需求文档：`MQDesk_PRD.md`
  - 设计规范：`DesignSystemManifest.md`
  - 开发交接：`DevHandoff.md`
- **反馈问题**：请在项目仓库提交 Issue，并附上截图和复现步骤。

---

> **手册版本记录**
>
> - v0.1.0：首次编写，对应 MQDesk MVP 版本。
