# MQDesk —— RabbitMQ 可视化管控台 产品需求文档（PRD）

> 文档版本：v1.0 ｜ 状态：Draft for Review ｜ 创建日期：2026-07-25
> 作者：产品通（代笔）｜ 评审人：（待补充）｜ 最后更新：2026-07-25

---

## 0. 产品一句话定位

**MQDesk 是一款面向"小白/初级用户"的 RabbitMQ 桌面可视化管控工具，让不懂 AMQP、看不懂英文管理后台的人，也能在 5 分钟内连接服务、看懂队列状态、发送并监听消息、一眼看出消息是否堆积或被消费。**

对标产品：[Another Redis Desktop Manager（ARDM）](https://github.com/RedisInsight/RedisInsight) 在 Redis 领域的地位——桌面客户端、连接即存、中文母语、可视化操作、零命令行。

---

## 1. 问题陈述（Problem Statement）

### 1.1 用户痛点
RabbitMQ 官方管理后台（`rabbitmq_management` Web UI）存在三个让新手劝退的硬伤：

1. **全英文 + 专业黑话**：界面满是 `Exchanges / Bindings / Routing Key / Unacked / Ready` 等 AMQP 术语，缺乏任何中文释义与引导，小白根本不知道每个东西"是干嘛的、点了会发生什么"。
2. **操作路径不直观**：发送一条测试消息需要自己拼 Exchange、Routing Key、Payload，没有任何模板和示例；"监听消费消息"更需要在命令行敲 `amqp-consume` 或写代码。
3. **状态看不懂**：队列列表里 `Ready / Unacked / Total` 只是一串数字，没有"是否堆积、是否被消费、消费速率"的直观解释和告警，出了问题只能干瞪眼。

### 1.2 谁会遇到、多频繁
| 角色 | 场景 | 频率 |
|------|------|------|
| 初级开发 / 实习生 | 联调时想发条消息验证链路 | 每天 |
| 测试工程师 | 手动构造消息做异常测试 | 每天 |
| 运维新人 | 排查队列堆积、确认消息是否被消费 | 每周 |
| 产品 / 技术小白 | 想"看一眼"系统里跑了什么消息 | 偶尔 |

### 1.3 不解决的代价
- **学习成本转嫁人力**：每个新人入职都要老手"手把手教 RabbitMQ"，隐性培训成本高。
- **排查效率低下**：线上消息堆积/未消费，靠命令行和英文后台排查，平均定位时间长、易误判。
- **竞争风险**：Redis 有 ARDM 这类国民级可视化工具，AMQP 生态长期缺"小白友好"的桌面客户端，存在产品空白可切入。

---

## 2. 目标（Goals）

### 用户目标（User Goals）
- **G1 零门槛上手**：小白首次使用，无需任何 AMQP 知识，5 分钟内完成"连接 → 看到队列 → 发送一条消息 → 看到它被消费"的完整闭环。
- **G2 状态一看就懂**：用户打开任意队列，能立刻回答三个问题——消息多不多（是否堆积）？有没有人在消费？消费得快不快？
- **G3 操作有安全感**：任何"会改动服务端"的操作（发消息、建队列、清队列）都有明确的中文说明 + 二次确认 + 可逆提示。

### 业务目标（Business Goals）
- **G4 降低支持成本**：新用户关于"RabbitMQ 怎么用"的工单/答疑量下降 ≥ 40%（上线 3 个月后对比）。
- **G5 提升采纳率**：目标用户在接入 RabbitMQ 项目中的工具采纳率（周活 / 项目人数）≥ 60%。

### 可衡量性说明
每条目标都对应第 9 节的指标与基线假设，避免"提升体验"这类空话。

---

## 3. 非目标（Non-Goals）

为避免范围蔓延，v1 **明确不做**以下事情：

| # | 非目标 | 原因 |
|---|--------|------|
| NG1 | 自研消息代理 / 替换 RabbitMQ | 我们做"管控界面"，不是做中间件，越界且无必要 |
| NG2 | 多租户企业级权限治理（SSO、RBAC、审计日志） | 面向个人/小团队提效，企业治理是独立大项目 |
| NG3 | 消息内容审计、敏感词扫描、合规留存 | 涉及数据合规与法务，需单独立项，v1 不碰用户数据内容 |
| NG4 | 集群性能调优、节点资源监控（CPU/内存/磁盘） | 那是运维监控平台（如 Prometheus）的事，本工具聚焦"消息与队列" |
| NG5 | 跨消息中间件统一管控（Kafka/RocketMQ/Pulsar） | v1 只做 RabbitMQ，架构预留扩展但不在本期实现 |
| NG6 | 消息的持久化存储与历史回溯（长期保存所有消息） | 工具只"展示当前状态"，不替代消息持久化方案 |

---

## 4. 用户画像（Personas）

1. **小白开发「小明」**：应届生，会写 CRUD，不懂 AMQP。需要"跟着提示点就能发消息"的引导。
2. **测试「婷婷」**：需要手动发各种 Payload（正常/异常/超大）验证消费端逻辑，要能反复发、能看消费结果。
3. **运维新人「阿强」**：线上告警说"队列堆积了"，他需要一眼看出哪个队列堆积、堆积多少、有没有消费者、消费速率多少。
4. **技术小白/产品「莉莉」**：偶尔想"看一眼"系统里消息长什么样，不需要操作，只要可读视图。

---

## 5. 用户故事（User Stories）

> 优先级从高到低。格式：`作为 [角色]，我希望 [能力]，以便 [价值]`。

- **US1（P0）**：作为小白开发，我希望用「地址+账号密码」一键连接 RabbitMQ，并能保存多个连接，以便下次直接打开、不用重输。
- **US2（P0）**：作为任何用户，我希望整个界面是中文的，且每个专业术语旁有「？」可悬浮看大白话解释，以便我不用查文档也懂每个东西干嘛的。
- **US3（P0）**：作为小白开发，我希望有一个「总览」页用一句话告诉我"当前有没有问题"（如：3 个队列堆积、2 个队列无人消费），以便我一眼掌握全局。
- **US4（P0）**：作为小白开发，我希望能用引导式表单发送一条消息（选队列/交换机、填内容、有示例），点完能看到"发送成功"，以便我确认链路通了。
- **US5（P0）**：作为测试，我希望能"抓取"队列里最新的 N 条消息预览（不真正消费掉），以便我排查消息内容又不影响线上消费。
- **US6（P0）**：作为运维新人，我希望打开队列详情能看到：待消费数、处理中数、消费者数、近 1 分钟收发速率，并有"堆积/未消费"红黄绿标识，以便我快速判断健康度。
- **US7（P0）**：作为任何用户，我希望有一个「消息通知列表」，按时间流展示我经本工具发送 / 接收的消息，并标注状态（已发送 / 已被消费 / 仍堆积 / 消费失败），以便我追踪每条消息的命运。
- **US8（P1）**：作为小白开发，我希望能"实时监听"某个队列，新消息进来时列表自动刷新弹出，以便我看实时消费过程。
- **US9（P1）**：作为运维，我希望队列堆积超过阈值时收到本地桌面通知（弹窗/红点），以便我不用一直盯着看。
- **US10（P1）**：作为小白，我希望"新建队列/交换机"有向导，自动帮我填好常用参数并解释每个参数的作用，以便我不踩坑。
- **US11（P2）**：作为高级用户，我希望能看 Exchange → Binding → Queue 的拓扑关系图，以便我理解消息路由路径。
- **US12（P2）**：作为测试，我希望能把一组消息保存为"测试场景"反复发送，以便回归测试。

---

## 6. 需求规格（Requirements）

### 6.1 术语中文化映射（贯穿全局，P0）
这是"小白能看懂"的基石，所有界面复用同一套映射：

| 英文术语 | 中文显示 | 悬浮解释（大白话） |
|----------|----------|--------------------|
| Broker | 消息服务节点 | 跑着 RabbitMQ 的那台服务器 |
| Virtual Host | 虚拟空间（vhost） | 一块隔离的"房间"，不同项目用不同房间互不干扰 |
| Exchange | 交换机 | 消息的"分拣中心"，按规则把消息送到对应队列 |
| Binding | 绑定规则 | 交换机和队列之间的"连线规则" |
| Routing Key | 路由键 | 贴在消息上的"地址标签"，决定它去哪个队列 |
| Queue | 队列 | 存消息的"桶"，消费者从这里取 |
| Ready | 待消费 | 已经躺在桶里、等着被取的消息数 |
| Unacked | 处理中 | 已被取走、但还没确认处理完的消息数 |
| Message | 消息 | 你要传递的那段数据（通常是 JSON） |
| Ack | 消费确认 | 消费者说"我处理完了"的回执 |
| Consumer | 消费者 | 从队列取消息来处理的程序 |
| Dead Letter | 死信 | 处理失败/过期被扔到"垃圾箱队列"的消息 |

### 6.2 P0 —— 必须实现（MVP，砍掉任一条都不算解决核心问题）

#### R1 连接管理
- 支持 AMQP 协议连接（host / port / vhost / username / password），支持 HTTP API 作为管理数据来源。
- 连接配置可保存、可编辑、可删除，列表展示；支持"测试连接"后再保存。
- 密码本地加密存储（不落明文）。
- **验收标准**：
  - [ ] 给定正确的连接信息，点击"连接"后 ≤ 3 秒进入总览页；
  - [ ] 信息错误时给出中文报错（如"连接被拒绝，请检查地址/端口"），不抛英文异常；
  - [ ] 已保存连接重启软件后仍在；密码不以明文出现在配置文件中。

#### R2 全局中文界面 + 术语提示
- 所有菜单、按钮、字段、状态均为中文。
- 每个 AMQP 术语旁有「？」图标，悬浮显示 §6.1 的大白话解释；关键操作前有"这一步在做什么"一句话说明。
- **验收标准**：遍历核心页面，无残留英文专业词（专有名词如 AMQP 可保留但须有解释）；术语悬浮 100% 覆盖 §6.1 列表。

#### R3 总览仪表盘（一句话健康度）
- 顶部用一句话总结："✅ 一切正常 / ⚠️ 3 个队列堆积 / 🔴 2 个队列无人消费"。
- 卡片展示：队列总数、交换机总数、消息总数、当前告警数。
- 点击告警可直达对应队列详情。
- **验收标准**：当存在 Ready 数 > 阈值的队列时，首页必须显示堆积告警；无异常时显示正常态。

#### R4 引导式发送消息
- 表单字段：目标（队列直发 / 经交换机+路由键）、消息内容（默认 JSON 编辑器，带格式校验）、优先级（可选）。
- 提供「示例消息」一键填充；发送前展示"这条消息将去往：队列 X"的预判提示。
- 发送成功/失败均有明确中文结果反馈，并写入消息通知列表（US7）。
- **验收标准**：小白按默认示例点"发送"即可成功；路由目标不存在时拦截并提示"队列 X 不存在，是否先创建？"；发送操作有二次确认弹窗。

#### R5 消息抓取预览（不消费）
- 支持对指定队列"取最新 N 条（默认 10）消息预览"，消息保留在队列中不被消费掉（使用 requeue）。
- 预览展示：消息体、路由键、入队时间、大小。
- **验收标准**：预览后队列 Ready 数不变（消息未被真正消费）；N 可配置（1~100）。

#### R6 队列详情与健康度
- 详情页展示：待消费(Ready)、处理中(Unacked)、总数、消费者数、近 1 分钟 收/发速率（来自 RabbitMQ 速率统计）。
- 健康度标识：
  - 🟢 正常：有消费者且 Ready 在低位；
  - 🟡 堆积预警：Ready > 阈值（默认 1000）且持续增长；
  - 🔴 无人消费：消费者数 = 0 且 Ready > 0；
  - ⚪ 空闲：Ready = 0。
- **验收标准**：上述四种状态均能正确判定并着色；速率数据每 5 秒刷新（可配）。

#### R7 消息通知列表（消息命运追踪）
- 独立面板/页签，按时间倒序展示经本工具**发送**和**接收/消费**的消息流。
- 每条记录字段：时间、方向（发/收）、目标队列、状态（已发送 / 已被消费 / 仍堆积 / 消费失败）、消息摘要。
- 状态判定逻辑：
  - *已发送*：成功 publish；
  - *仍堆积*：发送后一段时间内该消息仍在 Ready 中未被取走（按消息追踪标识判断，或按队列 Ready 增量推断）；
  - *已被消费*：被消费者取走并 ack（通过监听/消费确认反向标注，或队列 Ready 下降 + 有活跃消费者推断）；
  - *消费失败*：进入死信/被 reject。
- 支持按队列、状态、时间筛选。
- **验收标准**：用户经本工具发送的消息 100% 出现在列表；状态标签与实际服务端状态一致（以人工抽检为准，准确率 ≥ 95%）。

> ⚠️ R7 状态追踪的技术说明：RabbitMQ 原生不记录"某条具体消息的生命周期"。实现方式有两种，需在 Open Questions 中决策：(a) 通过 `message_id` 头注入追踪标识 + 订阅队列确认消费；(b) 以队列级 Ready/Unacked 变化 + 消费者活跃度做"推断式"状态。v1 建议先用 (b) 推断式，成本低、覆盖大部分场景。

### 6.3 P1 —— 应有（高优先级快速跟进）

| 需求 | 说明 | 验收要点 |
|------|------|----------|
| R8 实时监听消费 | 订阅队列，新消息到达时消息通知列表自动追加并高亮 | 监听中可随时停止；不重复消费同一条 |
| R9 桌面堆积告警 | Ready 超阈值时本地弹窗/红点通知 | 阈值可配置；不骚扰（同队列不重复弹） |
| R10 新建队列/交换机向导 | 分步向导，自动填常用参数并解释每项 | 小白按向导可建出可用队列；参数有中文说明 |
| R11 清空/删除保护 | 清空队列、删除队列有醒目二次确认 + 不可逆提示 | 误删概率趋零 |

### 6.4 P2 —— 未来考虑（本期不做，但架构预留）

- R12 Exchange→Binding→Queue 拓扑图（可视化路由路径）。
- R13 测试场景保存与批量重发。
- R14 多消息中间件扩展框架（Kafka 等，仅预留抽象层）。
- R15 消息体 Diff 对比（两次抓取同队列消息差异）。

---

## 7. 信息架构（功能模块）

```
MQDesk
├─ 连接管理
│   ├─ 连接列表 / 新建 / 编辑 / 删除 / 测试连接
│   └─ 已保存连接（加密存储）
├─ 总览（Dashboard）
│   ├─ 一句话健康度 + 告警入口
│   └─ 队列 / 交换机 / 消息 概览卡片
├─ 队列（Queues）
│   ├─ 队列列表（名称 / Ready / Unacked / 消费者 / 健康色）
│   ├─ 队列详情（速率图 + 健康度 + 操作）
│   ├─ 抓取预览消息（不消费）
│   └─ 实时监听（P1）
├─ 消息（Messages）
│   ├─ 发送消息（引导式）
│   ├─ 新建队列/交换机向导（P1）
│   └─ 消息通知列表（命运追踪）
└─ 设置
    ├─ 语言（中文锁定 v1）
    ├─ 堆积阈值 / 刷新频率
    └─ 关于 / 术语表
```

---

## 8. 关键交互说明（小白视角）

### 8.1 首次使用引导（First-Run Onboarding）
首次打开 → 引导连接（填地址账号）→ 连接成功弹出"🎉 已连上，先带你发一条测试消息？"→ 一键示例发送 → 自动跳到队列详情看"这条消息去哪了"。目标：首次 5 分钟闭环（呼应 G1）。

### 8.2 发送消息的"零知识"路径
选目标 → 系统自动推荐"直发队列"或"经交换机" → 点「填入示例」→ 看预判"将去往：队列 order_queue" → 确认发送 → 结果反馈 + 列表记录。全程不出现未解释的英文。

### 8.3 "消息是否堆积/被消费"的呈现
队列详情顶部用大号色块显示健康度；右侧速率曲线展示"进/出"两条线——两线都平 = 健康，进线远高于出线 = 堆积中，出线为 0 且 Ready>0 = 没人消费。配合消息通知列表的状态标签，双保险让小白"看懂"。

---

## 9. 成功指标（Success Metrics）

### 领先指标（上线后数天~数周）
| 指标 | 基线假设 | 成功阈值 | 拉长目标 | 测量方式 |
|------|----------|----------|----------|----------|
| 首次连接成功率 | 无数据，假设 70% | ≥ 90% | 95% | 埋点：连接事件成功/失败 |
| 首次发送消息成功率 | 假设 60% | ≥ 85% | 95% | 埋点：发送事件成功/失败 |
| 5 分钟闭环完成率 | 无 | ≥ 60% | 80% | Onboarding 完成埋点 |
| 术语提示点击率 | 无 | ≥ 40% 新用户点击过 | — | 悬浮提示曝光/点击 |
| 周活跃 / 项目人数 采纳率 | 无 | ≥ 60% | 75% | 活跃连接用户 / 总目标用户 |

### 滞后指标（数周~数月）
| 指标 | 目标 | 测量 |
|------|------|------|
| RabbitMQ 使用相关工单量 | 下降 ≥ 40% | 对比上线前后 3 个月支持系统 |
| NPS 变化 | 相对无工具基线 +15 | 上线 1 个月后问卷 |
| 消息堆积平均定位时长 | 下降 ≥ 50% | 运维排查耗时抽样 |

> 基线若无历史数据，上线首月定义为"采集期"，第 2 个月起正式评估。

---

## 10. 待解决问题（Open Questions）

| # | 问题 | 负责人 | 阻塞级别 |
|---|------|--------|----------|
| Q1 | R7 消息状态追踪用「注入追踪标识」还是「队列级推断」？影响准确度与复杂度 | 工程 + 产品 | **阻塞**（决定 R7 实现成本） |
| Q2 | 连接走 AMQP 还是 HTTP API，或两者结合？实时监听需 AMQP 消费者 | 工程 | 阻塞 |
| Q3 | 密码本地加密用何种方案（系统钥匙串 / 加密配置文件）？ | 工程 + 安全 | 非阻塞 |
| Q4 | 桌面端技术栈是否复用 Electron + Vue（同 ARDM）？ | 工程 | 非阻塞 |
| Q5 | "堆积阈值"默认值 1000 是否合适，是否按队列可单独配置？ | 产品 + 运维 | 非阻塞 |
| Q6 | 是否需要导出消息/连接配置（合规与数据出境风险）？ | 法务 + 产品 | 非阻塞 |

---

## 11. 时间线与分期建议

- **M1（v1.0 MVP）**：R1–R7 全量 + 首次引导。交付"小白能连、能发、能看、能懂"的最小闭环。
- **M2（v1.1 快跟）**：R8–R11（实时监听、桌面告警、新建向导、删除保护）。
- **M3（v2.0）**：R12–R15（拓扑图、测试场景、多中间件预留、消息 Diff）。

> 硬性 deadline：若绑定某项目上线节点，需在评审时回填；否则按上述 M1→M2→M3 顺序推进，M1 不依赖其他团队。

---

## 12. 风险与合规

- **数据合规**：本工具读取队列消息内容，属用户业务数据。v1 不做内容留存（NG3），但抓取预览、实时监听会在内存中短暂持有消息——需在隐私说明中告知用户，本地不落盘。
- **误操作风险**：发消息/清队列会真实影响服务端。所有写操作强制二次确认（R4/R11），并在文档与界面明示"操作会真实生效"。
- **连接安全**：默认不开启明文 HTTP，鼓励 AMQPS；密码加密存储（R1）。
